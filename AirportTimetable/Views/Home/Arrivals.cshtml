<!DOCTYPE html>
<html>
<head>
    <title>Прилеты</title>
</head>
<body>
    @section Header{
        @using (Ajax.BeginForm("Index", new AjaxOptions { UpdateTargetId = "maindiv" }))
        {
            <span class="maintitletext">Прилеты  </span>
            <span class="secondarytitletext">(Arrivals)</span>
            <input id="button" type="submit" />
            }
        }
</body>
</html>